extends PanelContainer


# The purpose of this script is to accept input from the scenario about where what
#map is available and where the player is on it. It will then draw the map and update
#whenever a directional button is pushed. It will need to move the map left, right, up,
#or down underneath a token that represents the player. 


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta: float) -> void:
#	pass
